import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.duocuc.app',
  appName: 'registapp2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
